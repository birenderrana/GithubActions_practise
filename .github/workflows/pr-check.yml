name: pull trigger

on:
    pull_request:
        types:
            - opened
            - synchronize
        branches: 
            - main
    
jobs:
    pr_check:
        runs-on: ubuntu-latest
        steps:
            - name: PR check running for branch
              run: echo "PR check runningg for branch:" ${GITHUB_REF##*/}

